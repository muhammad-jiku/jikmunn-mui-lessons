import { AppBar } from '@mui/material';
import React from 'react';

const Navbar = () => {
  return (
    <AppBar position="sticky">
      <h1>Navbar</h1>
    </AppBar>
  );
};

export default Navbar;
